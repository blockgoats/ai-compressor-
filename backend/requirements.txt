# Core dependencies
numpy>=1.21.0
transformers>=4.20.0
tokenizers>=0.12.0
torch>=1.9.0
scipy>=1.7.0
click>=8.0.0
tqdm>=4.60.0
huggingface-hub>=0.10.0

# Optional GPU acceleration
# cupy-cuda11x>=9.0.0  # Uncomment for GPU support

# Pro tier dependencies (installed separately)
# stripe>=3.0.0