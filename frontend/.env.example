# Backend (FastAPI)
# Leave empty or set to "proxy" to use same-origin /api (Vite proxies to VITE_PROXY_TARGET).
# Or set a full origin, e.g. http://127.0.0.1:8765
VITE_API_BASE_URL=
# Target for vite dev server proxy (server-side only)
VITE_PROXY_TARGET=http://127.0.0.1:8000
